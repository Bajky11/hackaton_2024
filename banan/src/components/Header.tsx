import { Stack } from '@mui/material';
import { header_height } from '@/constants';

const Header = () => {
  return <Stack bgcolor={'#ececec'} height={header_height}></Stack>;
};

export default Header;
